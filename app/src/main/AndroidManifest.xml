<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.r1diseo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.R1diseño">
        <activity android:name=".content_Crud"></activity>
        <activity
            android:name=".insertar"
            android:label="@string/title_activity_insertar"
            android:theme="@style/Theme.R1diseño.NoActionBar" />
        <activity android:name=".contenido" />
        <activity android:name=".singup" />
        <activity android:name=".login" />
        <activity android:name=".dashboard" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>